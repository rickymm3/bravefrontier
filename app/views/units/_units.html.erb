</br></br>
<%= will_paginate @posts %>
<div class="hidden search-value" data-search-value="<%= @search_phrase %>"></div>
<table class="table">
  <thead>
  <tr>
    <th>No</th>
    <th>Rarity</th>
    <th>Avatar</th>
    <th>Name</th>
    <%- if current_user %>
      <%- if current_user.is_admin? %>
    <th>Edit
      <%- end %>
    <%- end %>
  </tr>
  </thead>

  <tbody>
  <% @units.each do |unit| %>
    <tr>
      <td><%= sprintf '%03d', unit.no if unit.no %></td>
      <td><%= unit.rarity_id.to_s %> <i class='icon-star'></i></td>
      <td><%= link_to (image_tag (unit.avatar)), unit %></td>
      <td><%= link_to unit.name, unit %></td>
      <%- if current_user %>
      <td><%= link_to '<i class="icon-edit"></i>'.html_safe, edit_unit_path(unit) %></td>
      <%- end %>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Unit', new_unit_path %>

<%= will_paginate @posts %>
